# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/
.idea/

# Kiro spec files (not needed in the deployed repo)
.kiro/

# If you ever store real secrets locally, add them here:
# js/config.local.js

# Keep backup files but ignore very large ones
# backups/*.json are committed intentionally by the GitHub Action
